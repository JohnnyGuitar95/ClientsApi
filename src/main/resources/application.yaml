spring:
  datasource:
    url: jdbc:h2:mem:testdb
    password: 1
    username: admin
  h2:
      console:
        enabled: true
        path: /h2-console
  jpa:
      hibernate:
        ddl-auto: create-drop
springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  swagger-ui:
    path: /swagger-ui
    operations-sorter: method